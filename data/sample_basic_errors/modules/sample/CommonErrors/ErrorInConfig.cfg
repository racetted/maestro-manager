if [[ -f $SEQ_EXP_HOME/file_to_stop_ErrorInConfig_from_aborting ]] ; then 
  echo "Yay... the file is there." 

else 
  echo "Please run \"touch $SEQ_EXP_HOME/file_to_stop_ErrorInConfig_from_aborting\" to ... well you know." 
  exit 1
fi 
